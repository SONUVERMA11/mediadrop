<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme.
         Since this is a Compose app, the XML theme only needs to handle
         the initial window background and status/nav bar colors.
         The actual Material 3 theming is done entirely in Compose via MediaDropTheme. -->
    <style name="Theme.MediaDrop" parent="Theme.Material3.DayNight.NoActionBar" />

    <!-- Translucent theme for ShareReceiverActivity — appears as a transparent overlay -->
    <style name="Theme.MediaDrop.Translucent" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>
</resources>
